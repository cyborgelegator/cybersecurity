# find connect scans
tcp.flags.syn==1 and tcp.flags.ack==0 and tcp.window_size > 1024 

# find SYN scans
tcp.flags.syn==1 and tcp.flags.ack==0 and tcp.window_size <= 1024  

# find UDP scans
icmp.type==3 and icmp.code==3
